# 对称平方比值方法（Symmetric Square Ratio Method）详细讲解

## 1. 方法概述

本方法是一种基于**多维缩放（Multi-Dimensional Scaling, MDS）**的潜变量坐标初始化方法，核心思想是：**让低维空间中任意两点的距离与高维空间中对应两点的距离，在比值意义上保持对称一致**。

损失函数定义为：

```
L = mean( (d_high / d_low)² + (d_low / d_high)² )
```

其中：
- `d_high`：高维空间（原始曲线空间）中两点的距离
- `d_low`：低维空间（潜变量q空间）中对应两点的距离
- `mean`：对所有两两组合取平均

---

## 2. 为什么用这个损失函数？

### 2.1 对称性
无论 `d_high > d_low` 还是 `d_low > d_high`，损失值都是对称的：

```
(d_high / d_low)² + (d_low / d_high)² = (d_low / d_high)² + (d_high / d_low)²
```

这确保了"高估"和"低估"距离受到同等程度的惩罚。

### 2.2 尺度不变性
如果高维距离和低维距离同时放大或缩小相同倍数，损失值保持不变：

```
(k·d_high) / (k·d_low) = d_high / d_low
```

这意味着损失函数不关心整体尺度，只关心**相对比例**。

### 2.3 强惩罚极端偏差
当 `d_high / d_low` 远离 1 时（即比例严重失调），平方项会让损失值急剧增长：

| d_high / d_low | 损失值 (r² + 1/r²) |
|---------------|---------------------|
| 0.5           | 0.25 + 4.0 = 4.25  |
| 0.8           | 0.64 + 1.56 = 2.20 |
| 1.0           | 1.00 + 1.00 = 2.00 |
| 1.25          | 1.56 + 0.64 = 2.20 |
| 2.0           | 4.00 + 0.25 = 4.25 |

最小值在比值为 1 时取得（= 2.0），此时高低维距离完全匹配。

---

## 3. 完整训练流程（两阶段）

### 阶段一：MDS 预训练初始化 Q

1. **计算曲线距离矩阵**：对训练集中所有 label 的响应曲线，计算两两之间的 L1（曼哈顿）距离
   - 不对曲线做标准化（不减均值、不除RMS）
   - 不对距离矩阵做 z-score 标准化
   - 直接使用 L1 距离：`dist(x, y) = Σ|x_i - y_i|`

2. **平方比值 MDS 优化**：以随机初始化的 Q 为起点，通过梯度下降最小化上述对称平方比值损失，得到初始的潜变量坐标

3. **Q 坐标标准化**：对 MDS 输出的 Q 做列标准化（每列减均值、除标准差），确保各维度尺度一致

### 阶段二：固定 Q，训练 θ（神经网络参数）

1. **冻结 Q**：将阶段一得到的 Q 作为固定的 embedding，不参与梯度更新
2. **训练 θ**：仅优化神经网络 MLP 的参数，学习从 `(输入特征 x, 潜变量 q)` 到 `目标值 y` 的映射

---

## 4. 与原始方法的对比

| 方面 | 原始方法 | 新方法（对称平方比值） |
|------|---------|----------------------|
| 曲线标准化 | 减均值 + 除 RMS | **不做任何标准化** |
| 距离度量 | L2（欧氏） | **L1（曼哈顿）** |
| 距离矩阵标准化 | z-score | **不做** |
| Q 初始化 | 随机正态分布 | **MDS 预训练** |
| 训练方式 | Q 和 θ 一起训练 | **先训 Q，固定 Q 再训 θ** |
| MDS 损失函数 | 指数损失 exp(d/d0 + d0/d) | **对称平方比值 (d/d0)² + (d0/d)²** |

---

## 5. 关键超参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| MDS 学习率 | 0.05 | Adam 优化器的学习率 |
| MDS 迭代轮数 | 2000 | 梯度下降的总步数 |
| MDS 学习率调度 | CosineAnnealing | 余弦退火调度器，T_max=2000 |
| Q 初始化标准差 | 0.1 | MDS 优化起点的随机扰动幅度 |
| 梯度裁剪 max_norm | 1.0 | 防止 MDS 优化过程中梯度爆炸 |
| θ 训练学习率 | 5e-4 | 训练神经网络时的学习率 |
| θ 训练轮数 | 1000 | 训练神经网络的总 epoch 数 |
| 测试 Q 校准步数 | 200 | 对测试集 label 校准 Q 的迭代次数 |

---

## 6. 效果验证

在 12 个数学表达式数据集上的测试结果（数据量充分的情况下）：

| 数据集 | q_dim | 训练 R² | 测试 R² | Spearman |
|--------|-------|---------|---------|----------|
| 库仑定律 | 2 | 0.9997 | 0.9999 | 0.9206 |
| Arrhenius 反应速率 | 2 | 1.0000 | 1.0000 | 0.9649 |
| Michaelis-Menten | 2 | 0.9999 | 1.0000 | 0.9902 |
| Logistic 生长曲线 | 3 | 1.0000 | 0.9997 | 0.9476 |
| Gompertz 生长曲线 | 3 | 0.9999 | 0.9988 | 0.9450 |

平均测试 R² > 0.99，Spearman 相关系数 > 0.9，证明该方法在数据充分时能够有效学习潜变量空间的几何结构。
