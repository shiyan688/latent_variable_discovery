name: latent-variable-discovery

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - numpy
  - pandas
  - matplotlib
  - scikit-learn
  - sympy
  - pip:
      # Verify this resolves to a CUDA build on the target host. For explicit
      # CUDA wheel selection, follow the command in README.md instead.
      - torch
