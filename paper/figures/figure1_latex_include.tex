% Figure 1 is regenerated by scripts/generate_iclr_figure1_20260829.py.
\begin{figure*}[t]
  \centering
  \includegraphics[width=0.98\textwidth]{figures/figure1_variant_a.pdf}
  \caption{Raw latent coordinates can change under prediction-equivalent
  neural charts even when their physical curves agree (\textbf{a}).  We
  therefore identify an entity through its response on fixed physical probes
  (\textbf{b}) and project that response-equivalence class onto a named basis
  (\textbf{c}).  Support-only inference yields compact expressions that
  transfer to future thermoelectric and vapor-pressure entities
  (\textbf{d}); predictive superiority over local interpolation is evaluated
  separately.}
  \label{fig:canonical-overview}
\end{figure*}
