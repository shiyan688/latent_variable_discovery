# 潜变量搜索项目完整阶段性研究报告（外部读者版）

**报告日期：** 2026-08-09；终态更新于 2026-08-25

**报告性质：** 已完成实验的统一整理、统计复核与研究决策

**验证状态：** **PARTIALLY SUPERSEDED**（运行结果仍完整，但部分真实数据结论正在按 2026-08-25 证据审计重算）

> **2026-08-25 证据审计警告：** 后期 `application_full_features` Starry 数据实际以 composition 而非 sample ID 分组，并混入不同属性语义/温度坐标；较早 reviewer-clean electrical/thermal 也存在目标混杂。原 NASA prepared 数据同样失效：同一 B0025--B0028 文件被重复收录，其中三块电池以不同标签横跨 train/test，且输入包含同周期测得的最低电压、平均温度和平均电流。NASA 已用 18 个唯一 battery ID、四个前向可用条件和前段 support/后段 query 的 clean protocol 完成第一轮重算，结果见 4.0 节；旧 NASA 表格仍只保留为历史记录。Starry 相关正负结论仍须在 clean protocol 上重算；reviewer-clean Seebeck 降级为 provisional，ZT 不受本次问题影响。详见 [NEGATIVE_RESULT_REASSESSMENT_20260825.md](NEGATIVE_RESULT_REASSESSMENT_20260825.md) 和 [REAL_Q_SYMBOLIC_STRUCTURE_LOOP_MILESTONE_20260825.md](REAL_Q_SYMBOLIC_STRUCTURE_LOOP_MILESTONE_20260825.md)。在 Starry 主表完成重算前，不要引用本文关于多个真实数据集 kNN/RF 全面领先或 Starry 证明 q 失败的汇总句。

## 先用三分钟理解这项工作

### 我们在解决什么问题

许多科学对象不会只产生一个数，而会产生一条随条件变化的响应曲线。例如，电池容量会随循环次数下降，材料的热电性质会随温度变化。当出现一个从未见过的新电池或新材料时，我们通常只能先测到少量点，却希望预测其余条件下的完整响应。

本项目研究的问题是：**能否从一个新对象的少量已知观测中，推断一个低维“状态指纹”，再用它预测该对象剩余的响应？** 我们把这个状态指纹记作 \(q\)。它不是人工指定的电池健康度、材料类别或黏度，而是模型从数据中学习出来的连续向量。

以新电池为例：

1. 已知：前若干个循环的容量、充放电倍率和温度等观测；
2. 推断：用这些少量观测估计该电池的 \(q\)；
3. 预测：把“未来循环条件 + 该电池的 q”送入共享模型，预测后续容量曲线；
4. 检查：不仅看预测准不准，还看相似电池的 q 是否接近、换一个随机种子后 q 是否稳定。

这一设定可以统一描述电池、材料、气体传感器和参数化偏微分方程。文中的“实体”就是其中一个具体对象，例如一块电池、一个材料样品、一个采集批次或一个物理参数组合。

### 模型在训练和测试时做什么

**训练阶段：** 多个历史实体的观测 → 学习一个所有实体共享的预测器，同时为每个历史实体学习一个 q。

**测试阶段：** 新实体的少量已知点（support）→ 只调整新实体的 q → 在未知点（query）上预测响应。

因此，这不是“完全没见过目标值就预测”的零样本任务，而是**少样本曲线补全或系统辨识**。测试时只能使用 support 中的目标值来估计 q；query 的真实目标在预测完成前不可见。

这里的 q 也不是传统 VAE 中必须服从某个概率分布的随机变量。它更接近一个可优化的、低维的实体嵌入：训练时与共享网络共同学习，测试时根据新实体的 support 数据校准。

### 三个最重要的答案

1. **少量实体观测确实有用。** 潜变量模型相对完全不使用实体信息的 no-q MLP，在合成和论文范围真实数据上的配对中位误差分别降低约 94.4% 和 71.8%。
2. **但还不能说“显式 q 一定是最好的用法”。** support kNN 直接使用新实体的已知点，在多个真实数据集和 PDEBench 上比潜变量模型更准。1000 epoch、相同神经更新次数的补跑仍未消除这一差距。PDEBench 上 q=16 虽然在 10/10 seeds 中胜过 FPCA 和信息匹配 masked DeepONet，却同样在 10/10 中不及 kNN。
3. **当前最可靠的改进来自测试时校准。** 不只尝试一个 q 起点，而是尝试 4 个起点、用 support 内部数据选择并按条件精修，使指定真实任务的中位误差再下降 8.29%，但推断时间约变为 4 倍。

### 这项工作已经证明和没有证明什么

| 可以由当前实验支持 | 不能由当前实验支持 |
|---|---|
| 新实体的 support 信息对预测很重要 | 潜变量方法在每个数据集上都是最优方法 |
| 低维 q 可以形成有预测价值的实体表示 | q 已唯一恢复电池健康度、黏度等真实物理量 |
| 多起点校准能降低预测误差和尾部失败 | 显式 q 已优于所有 support-conditioned 方法 |
| 公共 PDEBench Burgers 上 q 明显优于 pooled MLP、support mean、FPCA 和本轮 masked DeepONet | q 已在 PDEBench 上超过 support-kNN 或所有可能的神经算子 |

### 如何阅读后面的报告

- 想先看结论：读第 1、4.3、15、19 和 20 节。
- 想理解任务和数据：读第 2 节。
- 想看每个数据集的完整数字：读第 3.3 和 4.3 节。
- 想研究 q 是否连续、稳定、可解释：读第 6 和 8 节。
- 想判断结论是否可靠：读第 13 和 14 节。

## 0. Material Passport

| 项目 | 状态 |
|---|---|
| 合成主实验 | 300/300 行：6 个表达式 × 10 种方法 × 5 个种子 |
| 论文范围真实数据主实验 | 315/315 行：7 个数据集 × 9 种方法 × 5 个种子 |
| 论文范围真实数据敏感性实验 | 819 个成功且唯一的运行；覆盖 q 维数、支持集比例、训练日程和正则化 |
| 精确 Burgers 机理实验 | 主实验 18 个模型任务、48 个方法/基线结果；另有 15 个正则筛选任务和 6 个独立确认任务 |
| 校准实验 | 合成和真实数据均完成筛选、机制修正、阈值敏感性及未见种子确认 |
| 外部 PDEBench | 核心实验、1000-epoch 匹配更新确认、FPCA 和信息匹配 masked DeepONet 均已完成 |
| 1000-epoch 公平性补跑 | 230/230 任务：4 个真实数据集的 5 方法 × 10 seeds，加 PDEBench 30 个训练任务；0 失败、0 超时 |
| PDEBench 函数空间基线 | 20/20 任务：FPCA 与 masked DeepONet × 10 seeds；0 失败、0 超时、逐行 query 对齐且泄漏探针为 0 |
| 数据来源 | 本地 `result.json`、CSV、checkpoint、预测值、q 值、manifest 和分析报告 |
| 外部上传 | 无 |
| 当前代码版本 | 基础提交 `2b13869`；研究代码存在未提交修改，尚未形成不可变发布版本 |
| 远端分支 | 仅 `origin/main`，与本地 `main` 和当前实验分支均指向 `2b13869` |

本报告把结论分成三层：

- **已证实：** 在当前协议、数据和模型预算内，有配对实验、效应量及重复种子支持。
- **提示性：** 方向一致或效应有研究价值，但样本量、多重比较或任务异质性不足以支撑普遍结论。
- **尚未验证：** 尚缺公平基线、外部数据或不可变版本复现，不作结果性表述。

## 1. 一页结论

### 1.1 目前最可靠的科学结论

1. **实体/系统特异的信息确实重要。** 在合成和论文范围真实数据上，使用支持集推断潜变量的模型显著优于把所有实体混在一起、完全忽略实体状态的 no-q MLP。合成数据中 `joint_mse` 对 no-q MLP 的配对中位改善约 94.4%，30/30 个表达式-种子块获胜；真实数据中配对中位改善约 71.8%，33/35 块获胜。
2. **但尚未证明“显式 q”是必要的。** 真实数据上的 support kNN 总体中位 NRMSE 为 0.0761，优于 `joint_mse` 的 0.1425，并在 28/35 块中胜过 `joint_mse`。1000-epoch 匹配更新实验仍显示：除 NASA battery 外，kNN 在三项 Starry 和 PDEBench 上 10/10 配对种子胜出。PDEBench 的新增公平基线进一步表明 q=16 continuity 在 10/10 seeds 中胜过 FPCA 和 masked DeepONet，但仍在 10/10 中不及 kNN。因此当前证据证明的是“支持集条件化有价值，且显式 q 对已声明函数基线有竞争力”，而不是“显式低维 q 必然优于所有支持集方法”。
3. **校准是当前最明确的提升点。** 对 `q_dim >= 2`，训练 q 先验初始化、4 个起点、支持集内部选择和有条件精修，使三个真实 q=4 任务的 NRMSE 中位下降 8.29%，34/36 块获胜，95% bootstrap CI 为 [-14.67%, -5.47%]；代价是约 4 倍校准时间。
4. **真实数据更偏好较高 q 维数。** 在论文范围的公平种子子集上，q=1 明显不足；q=4 相对 q=2 对 alternating 模型中位改善 23.6%，17/21 块获胜，Wilcoxon p=0.00488。对 joint 模型改善 18.6%，但 p=0.0888，只属于提示性证据。
5. **联合与交替优化没有稳定赢家。** 合成和真实数据上，alternating 有时均值更好，但配对检验均未形成稳定显著优势。它们应继续作为两种各有取舍的方案，而不是宣布其中之一普遍更优。
6. **当前成套正则化不应推广。** fixed/dynamic 正则组合在真实数据上显著损害预测，并普遍恶化局部几何失真。它们虽然改善部分跨种子 q 稳定性，但这不足以抵消主目标退化。
7. **连续性需要与预测、稳定性和失真共同解读。** q 与响应曲线几何连续，说明它是有用的低维描述符；它不能证明 q 唯一对应真实物理因子，也不能排除旋转、缩放、折叠或多对一编码。
8. **精确 Burgers 实验支持“预测性潜变量”和“物理可识别潜变量”是两个目标。** q=2 的预测最好，q=4 的物理嵌入与连续性更好；两者存在明确权衡。

### 1.2 当前最诚实的论文主张

> 给定未见实体的一小组支持观测，可以推断一个连续低维状态，用于预测该实体剩余响应；这个状态在多种科学曲线任务上显著优于支持盲基线，并呈现可测量的响应几何结构。多起点、支持内选择的测试时校准能稳定提升预测和尾部风险。但显式潜变量相对强 support-conditioned 无潜变量模型的必要性，以及 q 的唯一物理可识别性，仍待验证。

建议论文主线命名为：**Support-conditioned continuous latent state discovery for scientific response families**。

### 1.3 主要主张与证据对应

| 主张 | 直接证据 | 状态 |
|---|---|---|
| support 中的实体信息能显著改善预测 | 合成 `joint_mse` vs no-q：30/30 胜；论文范围真实：33/35 胜，均通过 BH 修正 | 当前协议内支持 |
| 多起点校准能改善 q=4 真实预测 | 三个数据集、36 个配对块：中位 -8.29%，34/36 胜；未见种子仍改善 7.25% | 指定任务内支持 |
| q=4 比 q=2 更适合真实预测 | alternating：17/21 胜、p=0.00488；joint 方向一致但 p=0.0888 | 提示性，不宜泛化到所有任务 |
| 成套 fixed/dynamic 正则总体有益 | 真实主实验预测和局部失真普遍退化 | 不支持，已否定当前配置 |
| 显式 q 优于所有使用 support 的方法 | support kNN 经常更强；DeepSets/CNP/可靠性选择器也未给出显式 q 的普遍优势 | 尚未验证 |
| q 唯一恢复真实物理状态 | Burgers 中预测最佳与物理对齐最佳配置不同 | 不支持唯一恢复 |
| 方法在公共 PDEBench Burgers 上具有预测价值 | 匹配更新的 q=16 continuity 中位 NRMSE 0.2573，10/10 胜 FPCA 0.2753 和 masked DeepONet 0.3136，但 10/10 不及 support-kNN 0.2113 | 指定 PDEBench 任务内支持 |

## 2. 问题、数据和评价协议

### 2.1 从一个电池例子到一般任务

假设历史数据中有许多电池的容量衰减曲线，现在得到一块训练时从未出现过的新电池。我们先测量其一部分循环，称为 **support set（支持集）**；剩余需要预测的循环称为 **query set（查询集）**。模型必须从 support 推断新电池的状态，再预测 query。材料和传感器数据采用相同结构，只是“实体”“输入条件”和“响应”不同。

该任务同时要求三个能力：

1. **准确：** 少量观测后能否预测剩余响应；
2. **稳健：** 换初始化、随机种子或支持点后是否仍得到相近结论；
3. **有结构：** 响应相似的实体是否在 q 空间中相邻，而不是被随意打散。

### 2.2 数学定义

实体 \(i\) 的支持集写作

\[
S_i=\{(x_{ij},y_{ij})\}_{j=1}^{m_i},
\]

其中 \(x\) 是查询条件，如循环次数、温度或空间—时间坐标；\(y\) 是容量、材料性质或传感器响应。模型根据 \(S_i\) 估计实体潜变量 \(q_i\)，再由共享预测器输出

\[
\hat y=f_\theta(x,q_i).
\]

训练、验证和测试按**实体标签**隔离，而不是把同一实体的行随机拆到两边。这样测试集确实包含未见实体。测试时只允许用 support 的 \(y\) 校准 q，query 的 \(y\) 只用于最终评分。

### 2.3 七个论文范围真实数据集分别代表什么

| 数据集 | 一个“实体”是什么 | 输入条件 x | 要预测的 y | 准备数据规模 |
|---|---|---|---|---:|
| MATR battery | 一块电池/对应循环协议 | 循环次数、充电倍率、充电比例、放电倍率 | 容量 | 38,765 行，46 个实体 |
| NASA battery（reviewer-clean） | 一个去重后的真实 battery ID | 放电序号、环境温度、名义负载电流、文档记录的截止电压 | 放电容量 | 1,635 行，18 个实体（13 train / 5 outer-test） |
| Starry electrical | 一个材料样品 | 温度与组成统计量 | 电导率 | 6,514 行，80 个实体 |
| Starry seebeck | 一个材料样品 | 温度与组成统计量 | Seebeck 系数 | 8,981 行，80 个实体 |
| Starry thermal | 一个材料样品 | 温度与组成统计量 | 热导率 | 10,707 行，80 个实体 |
| Starry zT | 一个材料样品 | 温度与组成统计量 | 热电优值 zT | 5,216 行，80 个实体 |
| UCI gas drift | 一个采集批次 | 气体类别、浓度、传感器和特征编号 | 对数传感器响应 | 285,824 行，10 个实体 |

“准备数据规模”描述清洗后数据，不等于主实验实际使用的全部行数。广泛真实主实验为了控制计算量，对每个实体设置了行数上限，因此它仍是筛选性基准实验；最终论文需要全行或预先冻结的分层样本确认。

### 2.4 术语速查

| 术语 | 非专业解释 |
|---|---|
| 实体（entity） | 一块电池、一个材料样品、一个采集批次或一个物理参数组合 |
| support | 新实体中允许模型查看目标值的少量已知点 |
| query | 模型必须预测、评分前不可查看目标值的点 |
| q / 潜变量 | 用少数连续数字概括一个实体状态的向量；含义由数据学习，不预先命名 |
| q_dim | q 中数字的个数；q=1、2、4 分别表示一、二、四维状态指纹 |
| 校准（calibration） | 固定共享预测器，用新实体的 support 数据寻找适合该实体的 q |
| joint | 网络参数和训练实体 q 放在同一优化过程中更新 |
| alternating | 交替更新共享网络和训练实体 q |
| fixed/dynamic | 在预测损失外加入连续性、独立性、q 范数等约束的两个组合版本 |
| 随机种子（seed） | 改变初始化和随机采样的一次独立重复；用来检查结果是否偶然 |
| baseline | 用作比较的已有或更简单方法 |

### 2.5 如何读评价指标

- **NRMSE（主指标，越低越好）：** RMSE 除以训练目标的标准差。0 表示完美预测；0.1 表示误差约为训练响应自然波动的 10%；1 表示误差约为一个训练标准差。由于测试分布可能变化，它不是百分比误差。
- **中位数 [Q1, Q3]：** 五次随机重复的中央结果和中间 50% 范围，比单次最好结果或容易被离群值影响的均值更稳健。
- **胜率：** 在相同数据集/表达式和种子上，方法 A 的误差低于方法 B 的配对比例。
- **p95：** 第 95 百分位数，用来观察最差一端的尾部风险；例如 p95 误差下降表示大多数困难实体的上尾失败减轻。
- **95% CI：** 效应估计的不确定范围；越窄说明估计越精确。
- **Wilcoxon p：** 配对差异与“中位差为零”是否相容。它不表示方法获胜的概率，也不能代替效应大小。
- **BH q：** 对同时比较多个方法后的 p 值修正，用于降低“比较得越多，偶然显著越多”的风险。
- **continuity / trustworthiness（越高越好）：** 响应相似的实体在 q 空间中是否仍互为邻居。
- **local distortion（越低越好）：** q 空间是否把局部相似实体过度拉开或压到一起。
- **aligned q NRMSE / CCA：** 在合成数据知道真实物理因子时，先消除坐标旋转、缩放等任意性，再评价学习 q 与真因子的误差和典型相关性。

真实数据没有“真 q”，所以只能判断 q 是否有预测价值、是否保持响应邻域、是否跨种子稳定，不能直接证明它就是某个唯一物理量。

### 2.6 比较方法和它们回答的问题

| 方法 | 简单解释 | 它回答的问题 |
|---|---|---|
| `joint_mse` | 同时学习共享 MLP 和每个训练实体的 q，只优化预测误差 | 最简单的显式 q 是否已经有用？ |
| `alternating_mse` | 轮流更新共享 MLP 与 q | 把两类变量分开优化是否更稳？ |
| `joint_fixed` / `alternating_fixed` | 在预测外固定加入 q 独立性、响应连续性、q 范数和先验等约束 | 人工规定的几何约束是否改善 q？ |
| `joint_dynamic` / `alternating_dynamic` | 与 fixed 使用同一组约束，但训练中自动调整其相对尺度 | 自动平衡多种损失是否优于固定权重？ |
| no-q global MLP | 所有实体共用一个 MLP，只看 x，不看实体和 support | 如果完全忽略实体差异会怎样？ |
| Random Forest | 只根据输入特征预测的树模型 | 非神经表格模型能否更好？ |
| support kNN | 直接在新实体已知点附近做局部预测，不学习 q | 简单使用 support 能达到什么水平？ |
| DeepSets direct | 对 support 中的 `(x,y)` 编码后做全局平均，再直接预测 query | 可学习的、无显式 q 的全局 support 表示是否足够？ |
| attentive CNP | 每个 query 对 support 点做交叉注意力，再直接预测 | 局部 learned support conditioning 能否逼近或超过 kNN？ |
| bounded support-relative CNP | 用 support 的中位数/MAD 表示目标，并限制标准化输出幅度 | 遇到训练/测试量纲和重尾错配时，能否阻止神经网络灾难外推？ |
| reliability selector | 只用 support 内部验证点，在 global CNP 与 bounded CNP 间逐实体选择 | 能否同时保留普通任务精度和极端分布的安全性？ |
| true-q-conditioned MLP | 合成/PDE 中把真实生成参数直接交给有限容量 MLP | 已知真实参数时的参考表现如何？它不是理论上限 |

前三组潜变量方法都显式构造 q；support kNN、DeepSets 和 CNP 使用 support 但不在测试时优化显式 q；no-q MLP 和 Random Forest 是支持盲方法。learned support 基线已经完成开发性比较，但只有三个数据集、三个种子，且所有新方案的一般推进门槛均未通过，所以仍不能据此宣布哪一种表示机制普遍最好。

### 2.7 每组实验分别回答什么

| 实验 | 为什么要做 | 能回答的问题 | 不能单独回答的问题 |
|---|---|---|---|
| 合成表达式 | 生成过程和真实隐藏因子已知 | 方法能否预测、是否在部分任务恢复已知 q | 能否适应真实噪声和采集偏差 |
| 七个论文范围真实数据集 | 覆盖电池、材料和传感器 | 对未见真实实体是否有预测价值 | q 是否等于唯一物理状态 |
| q 维数/支持比例 | 改变状态容量和可用观测量 | q 太小是否欠拟合、更多支持点是否有用 | 最佳设置能否推广到所有任务 |
| 校准研究 | 只改变测试时寻找 q 的方法 | 性能瓶颈是否来自 q 推断失败 | 共享预测器本身是否最优 |
| 精确 Burgers | 方程、解和两个真实参数均已知 | 预测与物理参数恢复是否一致 | 公共 PDE 数据上的外部泛化 |
| PDEBench | 公共 PDE 外部验证 | 可检查更真实数值场景中的预测价值 | 仍是数值模拟，不能替代现场真实系统 |

## 3. 合成数据：在“知道隐藏答案”的环境中检验

合成实验的价值在于我们知道数据由哪些隐藏参数生成，因此不仅能看预测误差，还能检查学习到的 q 是否接近真实隐藏因素。它相当于先在有标准答案的环境中做机制测试，再进入真实数据。

### 3.1 总体预测

以下是 6 个表达式、5 个种子上的 NRMSE 汇总：

| 方法 | 均值 | 中位数 |
|---|---:|---:|
| `joint_mse` | 1.0791 | 0.0379 |
| `joint_dynamic` | 1.0975 | 0.0287 |
| `joint_fixed` | 1.1060 | 0.0368 |
| `alternating_fixed` | 1.1266 | 0.0438 |
| `alternating_dynamic` | 1.1699 | 0.0619 |
| `alternating_mse` | 1.1919 | 0.0497 |
| support kNN | 1.3736 | 0.0649 |
| finite true-q MLP | 1.5344 | 0.7366 |
| no-q MLP | 1.5539 | 0.6812 |
| Random Forest | 1.7273 | 0.7432 |

均值远高于中位数，主要由高动态范围、刚性的 expression 41 拉高。因此总体均值不能代替分表达式分析。

### 3.2 核心配对比较

| 对比：`joint_mse` 相对基线 | 配对块 | 中位相对改善 | `joint_mse` 胜率 | Wilcoxon p | BH q | 判断 |
|---|---:|---:|---:|---:|---:|---|
| no-q MLP | 30 | 94.40% | 30/30 | 1.86e-9 | 8.38e-9 | 强证据 |
| Random Forest | 30 | 95.95% | 30/30 | 1.86e-9 | 8.38e-9 | 强证据 |
| support kNN | 30 | 42.14% | 19/30 | 0.00466 | 0.0105 | 有效但异质 |
| finite true-q MLP | 30 | 33.32% | 22/30 | 0.00256 | 0.00768 | 有效；不是超越理论 oracle |
| `alternating_mse` | 30 | 11.38% | 21/30 | 0.0919 | 0.1379 | 未确认日程优劣 |

`joint_fixed`、`joint_dynamic` 相对 `joint_mse` 的中位原始差分别约 -0.00115、-0.00150，但 p=0.871、0.371，未形成可推广优势。

### 3.3 每个合成表达式的完整结果

下表单元格均为 seeds 0–4 的 `reference_nrmse` **中位数 [Q1, Q3]**（每格 n=5），越低越好。`†` 表示该表达式上最好的潜变量方法，粗体表示跨潜变量方法和基线的全局最佳；加粗仅作描述，显著性判断以 3.2 节的配对检验为准。数据来自 `synthetic_all_runs.csv`，没有挑选种子。

#### 潜变量方法

| 表达式 | J-MSE | A-MSE | J-Fixed | A-Fixed | J-Dynamic | A-Dynamic |
|---|---:|---:|---:|---:|---:|---:|
| Expression 3 | **0.0038 [0.0037, 0.0041]†** | 0.0050 [0.0044, 0.0054] | 0.0160 [0.0142, 0.0360] | 0.0159 [0.0137, 0.0338] | 0.0295 [0.0133, 0.0390] | 0.0250 [0.0137, 0.0652] |
| Expression 15 | 0.0038 [0.0038, 0.0041] | 0.0049 [0.0043, 0.0065] | 0.0027 [0.0018, 0.0030] | 0.0106 [0.0084, 0.0222] | 0.0025 [0.0019, 0.0036]† | 0.0208 [0.0103, 0.0263] |
| Expression 21 | 0.0759 [0.0724, 0.0796] | 0.0815 [0.0665, 0.0866] | 0.0412 [0.0376, 0.0544] | 0.0419 [0.0419, 0.0458] | 0.0279 [0.0263, 0.0719]† | 0.0619 [0.0497, 0.0627] |
| Expression 36 | 0.0053 [0.0052, 0.0057] | 0.0061 [0.0058, 0.0072] | 0.0048 [0.0045, 0.0053] | 0.0105 [0.0097, 0.0721] | **0.0047 [0.0045, 0.0049]†** | 0.0146 [0.0106, 0.0184] |
| Expression 41 | 7.091 [3.830, 7.491] | 7.345 [5.752, 7.998] | 7.153 [3.961, 8.104] | 7.315 [4.050, 7.944] | **6.817 [4.142, 7.955]†** | 7.337 [4.712, 8.163] |
| Expression 48 | 0.3841 [0.3693, 0.4146] | 0.3949 [0.3567, 0.4808] | **0.2983 [0.2867, 0.3077]†** | 0.3544 [0.3397, 0.3663] | 0.3076 [0.2796, 0.3166] | 0.3638 [0.3160, 0.3750] |

缩写：`J` = joint，`A` = alternating。

#### 基线方法

| 表达式 | support kNN | finite true-q MLP | no-q MLP | RF |
|---|---:|---:|---:|---:|
| Expression 3 | 0.1091 [0.1086, 0.1231] | 0.0045 [0.0044, 0.0046] | 0.6896 [0.6835, 0.6940] | 0.8093 [0.8078, 0.8171] |
| Expression 15 | **0.0002 [0.0001, 0.0002]** | 1.406 [1.266, 1.576] | 0.2365 [0.2314, 0.2527] | 0.5645 [0.5587, 0.5776] |
| Expression 21 | **0.0159 [0.0119, 0.0189]** | 0.1589 [0.1548, 0.1679] | 0.8700 [0.8698, 0.8701] | 1.022 [1.019, 1.024] |
| Expression 36 | 0.0139 [0.0112, 0.0178] | 1.324 [1.179, 1.483] | 0.3612 [0.3342, 0.3659] | 0.5943 [0.5907, 0.6156] |
| Expression 41 | 7.229 [7.001, 8.368] | 7.265 [3.722, 7.441] | 7.829 [4.239, 8.052] | 7.994 [4.414, 8.301] |
| Expression 48 | 0.7007 [0.6674, 0.7073] | 0.3572 [0.3396, 0.3573] | 0.6583 [0.6355, 0.6712] | 0.6564 [0.6281, 0.6801] |

逐表达式结果显示：潜变量方法在 expressions 3、36、41、48 上取得最低五种子中位数；support kNN 在 expressions 15、21 上更好。Expression 41 即使最优方法也达到 6.817，且 IQR 很宽，说明所有方法都未解决该刚性、高动态范围压力测试。Fixed/dynamic 的局部胜利集中在特定表达式，并不构成总体正则化优势。

**结论：** 合成结果证明方法能恢复可预测的实体状态，但表达式 41 表明当前模型对刚性、高动态范围和外推仍不稳健。不能只报总体中位数掩盖这一失败模式。

## 4. 真实数据：对未见科学实体的预测

真实实验把整个电池、材料样品或采集批次留到测试阶段。模型在训练时从未见过这些实体，只能依靠其 support 点估计状态。这里的主问题是实际预测价值，而不是物理参数恢复，因为真实数据通常没有唯一的“真 q”标签。

### 4.0 Reviewer-clean NASA blocked-cycle 重算（当前可用 NASA 证据）

旧 NASA prepared 数据有重复电池跨 split 和同周期响应特征泄漏，因此下表完全替代旧 NASA 数值。新协议只保留 18 个唯一电池和预测前可知的四个条件；每个 outer-test 电池只用最早 30% cycles 作 support，其余更晚 cycles 作 query。表中为 seeds 0--4 的 reference NRMSE 中位数 `[Q1, Q3]`，越低越好。

| 方法 | q 维数 | NRMSE 中位数 [Q1, Q3] | 对关键基线的五种子结果 |
|---|---:|---:|---|
| support kNN | — | **0.5934 [0.5934, 0.5934]** | 5/5 胜过所有 q 配置 |
| joint MSE | 4 | 1.0855 [0.9001, 1.1709] | 5/5 胜 no-q 与 RF；0/5 胜 kNN |
| joint continuity | 4 | 1.0980 [0.9531, 1.3159] | 5/5 胜 no-q 与 RF；0/5 胜 kNN |
| joint MSE | 2 | 1.3422 [1.1922, 1.6328] | q=4 在 4/5 seeds 更好 |
| joint continuity | 2 | 1.8763 [1.4139, 1.9239] | q=4 在 4/5 seeds 更好 |
| Random Forest | — | 1.8015 [1.7901, 1.8282] | support-blind |
| no-q MLP | — | 1.8879 [1.7710, 1.9557] | support-blind |

这张表支持两件不同的事。第一，q=4 相对 no-q MLP 和 Random Forest 的实体信息价值在 5/5 seeds 中成立；第二，显式 q 并不是该 blocked 任务的最佳纯预测器，prefix-support kNN 仍低约 45%。按种子配对计算，q=4 MSE 和 continuity 相对 kNN 的 NRMSE 中位数分别高 82.94% 和 85.04%。continuity q=4 也只在 1/5 seeds 中胜过 MSE q=4，因此不能把连续性损失写成预测改进。

表示质量给出另一面证据：continuity q=4 的训练实体 q-distance 几何跨 seed Spearman 中位数为 0.996，而 MSE q=4 为 0.287。其 decoder 功能坐标跨 seed Spearman 中位数为 0.799（cycle-1 capacity）、0.692（cycle-28 capacity）、0.692（early fade）和 0.662（mid fade）；cycle-1 功能坐标与训练电池经验 cycle-1 capacity 的相关中位数为 0.934。样本只有 13 个训练电池，这些数字是结构发现线索，不是已确认的物理定律。当前最诚实的结论是：continuity q 牺牲部分预测，却显著稳定了跨 seed 的实体几何和可命名功能坐标；下一步价值要由冻结 inner splits 上的符号结构复现来检验。

冻结的三组 8 meta-fit / 5 structure-validation 内层划分现已完成 30/30 q=4 单元。下表同时给出五种子结构验证 NRMSE 和每个划分十个 seed-pair 的训练 q-distance Spearman 中位数。内层划分是同一 NASA cohort 的开发切分，不是三个独立数据集。

| 内层划分 | continuity NRMSE 中位数 [Q1, Q3] | MSE NRMSE 中位数 [Q1, Q3] | continuity / MSE q-distance 稳定性 |
|---|---:|---:|---:|
| inner0 | 1.3861 [1.0027, 1.4635] | **0.9175 [0.6426, 1.0433]** | **0.996716** / 0.485769 |
| inner1 | 1.3782 [1.1585, 1.4869] | **0.9080 [0.7085, 0.9520]** | **0.999453** / 0.308429 |
| inner2 | 1.1694 [1.0808, 1.1932] | **1.0725 [0.8378, 1.5397]** | **0.998905** / 0.390805 |

continuity 只在 4/15 个“划分×seed”预测单元中胜过 MSE，配对 NRMSE 中位差为 +0.342803（+32.86%），再次确认预测与表示稳定性的取舍。但 cycle-1 capacity 和 early-fade 两个 decoder-derived 坐标的跨 split 稳定性中位数达到 0.904762 和 0.821429，并在 3/3 splits 中都比 MSE 更稳定；它们与对应训练曲线描述符的匹配相关跨 split 中位数分别为 0.880952 和 0.523810。mid-fade 在一个 split 的经验对齐降至 0.0714，fade acceleration 的匹配相关仅 0.1190，因此首轮符号结构发现只把 cycle-1 capacity 与 early fade 作为主候选，不从单个漂亮 seed 选择公式。

Outer 原始终态表位于 `runs/nasa_battery_reviewer_clean_anchor_20260825/all_runs.csv`；inner 训练与功能坐标报告位于 `runs/nasa_battery_reviewer_clean_inner_q_20260825/INNER_Q_FUNCTIONAL_RESULTS.md`。该 cohort 已被开发过程暴露，因此这仍是 development evidence；冻结结构后的最终论文确认需要外部或新隔离电池。

同一组三个 inner splits 的 Stage C 符号结构比较也已按冻结协议完成 90/90 cells。每条公式只在 8 个 meta-fit batteries 的后 70% cycles 拟合，并在 5 个 structure-validation batteries 的后 70% cycles 评分；validation q 与 support summaries 只使用前 30% target。90 个指标和预测均有限，8/5 实体隔离与前缀顺序通过，30 个 q source 的 query-target perturbation 最大输入差为 0。下表为 15 个“inner split × seed”单元的 structure-validation reference NRMSE，公式复杂度是 PySR complexity；越低越好。

| q 训练目标 | 符号接口 | NRMSE 中位数 [Q1, Q3] | 最大 NRMSE | NRMSE>10 | 复杂度中位数 | validation 输入最大 \|z\| 中位数 |
|---|---|---:|---:|---:|---:|---:|
| baseline | conditions only | **0.9365 [0.8551, 0.9850]** | 0.9850 | 0/15 | 11 | 2.23 |
| baseline | conditions + support summaries | 1.0332 [0.8578, 1.1897] | 1.5845 | 0/15 | 12 | 4.58 |
| continuity q | conditions + functional q | 1.7552 [0.7779, 2.5653] | 151.22 | 1/15 | 13 | 7.37 |
| continuity q | conditions + raw q | 3.0812 [1.4953, 40.5157] | 2.62e13 | 6/15 | 11 | 22.19 |
| MSE q | conditions + functional q | 0.9333 [0.8282, 3.4164] | 25.30 | 2/15 | 14 | 4.42 |
| MSE q | conditions + raw q | 1.2982 [0.8089, 4.1554] | 6.36e44 | 2/15 | 11 | 12.38 |

按 inner split 展开的五种子 NRMSE 中位数如下。它说明 aggregate 中位数不能掩盖 split 异质性：functional q 在 inner0/inner2 有若干接近 baseline 的结果，但 inner1 明显失配；continuity raw q 在 inner2 出现严重尾部。

| q 训练目标 / 符号接口 | inner0 | inner1 | inner2 |
|---|---:|---:|---:|
| baseline / conditions only | 0.8133 | **0.9850** | **0.9192** |
| baseline / support summaries | 0.8077 | 1.2631 | 0.9913 |
| continuity / functional q | 0.7587 | 2.7162 | 0.9993 |
| continuity / raw q | 0.9910 | 2.3461 | 68.8946 |
| MSE / functional q | 0.8738 | 3.3476 | 0.9571 |
| MSE / raw q | **0.7520** | 4.0386 | 0.9454 |

冻结 gate 总判定为 **3/5 PASS、overall FAIL**。完整性通过；continuity functional-q 未通过下游价值 gate，因为其中位数不低于两种 baseline，且对 condition-only 和 support summaries 都只有 4/15 配对胜场；可读性也未通过，因为 functional complexity 13 高于 raw-q 11。另一方面，宽 motif——公式同时使用 `discharge_index` 和 cycle-1 capacity / early fade 中至少一个——在 continuity 中出现 12/15，三个 splits 分别为 3/5、5/5、4/5，因此 motif recurrence 通过；MSE 为 11/15。functionalization 对 continuity raw q 有 10/15 胜场，将中位 NRMSE 从 3.0812 降至 1.7552。

这些结果不能被写成“q 没有符号价值”，也不能被挑选漂亮 seed 后写成成功。物理 condition 在 validation 中最多约 2.24 个 meta-fit 标准差，而 support-calibrated raw q 达 10--35 个标准差；PySR 把域外 q 放进指数或小分母后产生极端有限预测。更根本的协议不匹配是：meta-fit q 来自完整训练曲线上的联合优化，structure-validation q 来自冻结 decoder 上的 30% prefix-support 反演。Stage C 当前否定的是这个无约束、信息不匹配的接口。下一步应在新冻结切分上让 meta-fit q 也经过相同 support-only calibration，审计 support Jacobian 条件数和训练流形距离，并使用有界 functional vocabulary；在此通过前不能把宽 motif 直接升级为结构化 decoder。终态分析位于 `runs/nasa_battery_reviewer_clean_inner_symbolic_20260825/STAGE_C_ANALYSIS.md`，训练动力学推导位于 `NEURAL_TRAINING_DYNAMICS_FOR_LATENT_Q_20260825.md`。

#### 4.0.1 Stage C 接口诊断：为什么找到 q 仍可能无法得到稳定公式

上面的失败首先提出一个接口问题：训练电池的 q 看过完整曲线，测试电池的 q 却只能看前 30% 曲线。若两种 q 不在同一坐标区域，符号公式即使在训练电池上合理，也会在测试电池上外推。为逐层排除原因，我们保持同一组 3 个 inner splits、5 个 seeds 和 8/5 电池划分，依次做了四个冻结诊断。表中的 NRMSE 都是 continuity q 在 15 个“划分×seed”单元上的中位数；`raw z` 和 `functional z` 是测试 q 相对训练 q 范围的最大标准化偏移，越小越安全。

| q 接口 | 改变了什么 | NRMSE 中位数 | raw z 中位数 | functional z 中位数 | functional z≤6 | 冻结结论 |
|---|---|---:|---:|---:|---:|---|
| support-matched 反演 | 训练、测试都只用前 30% 重新估计 q；decoder 不变 | **1.193** | 9.709 | 4.282 | 11/15 | 偏移减小但尾部未清除 |
| 凸包 q | 测试 q 限制为 8 个训练 q 的凸组合 | 1.696 | 2.321 | 2.351 | 15/15 | 几何通过，预测保持失败 |
| 坐标盒 q | 只把每个测试 q 坐标裁到训练 q 的最小/最大值 | 1.702 | **2.276** | 2.436 | 15/15 | 几何通过，预测保持失败 |
| prefix-q 训练 | 训练时 q 也只看前 30%，theta 仍看完整曲线 | 1.387 | 8.815 | 3.389 | 12/15 | 10/15 配对单元改善，但尾部与稳定性 gate 失败 |

这四行把两个可能混在一起的问题分开了。第一，域外 q 确实是公式爆炸的重要直接原因：凸包和坐标盒都把 15/15 单元带回安全范围。第二，**事后把 q 拉回范围并不能保留 decoder 所需的信息**：两种不同约束几乎产生相同的约 1.70 NRMSE。prefix-q 训练则不靠事后投影，并在 10/15 单元中改善旧 support-matched 接口，说明训练信息对齐的方向有信号；但它还有五个明显退化单元，不能按中位配对改善就宣布成功。

对这五个尾部单元的事后归因没有发现一个可据此调参的简单阈值。continuity 在三个 splits 中分别有 3/5、3/5、4/5 单元保持在旧接口的 10% 内；其 NRMSE 比率与 raw-q 偏移的 Spearman 相关为 -0.329（p=0.232），与 functional-q 偏移为 0.104（p=0.713）。更值得注意的是，continuity 与 MSE 的退化大多不重合：以“比各自旧接口差超过 10%”为界，15 个单元中只有 1 个两者同时失败，另有 11 个只失败一种 loss；两种 loss 的 NRMSE 比率相关为 -0.489（p=0.064）。这些都是小样本、事后探索，不能升级为确认性结论，但它们提示几何漂移之外还存在 loss/optimizer 特异的吸引域或 gauge 选择问题，不能指望一个测试时边界单独消除全部长尾。

目前更精确的诊断是优化动力学和坐标尺度仍不匹配，而不是“真实数据没有可用 q”。训练 q 以学习率 0.001 与 decoder 共同演化约 3,000 步；测试 q 则以学习率 0.05 在冻结 decoder 上独立优化 200+50 步。prefix-q 的训练 q 跨 seed 距离几何仍很稳定（split 中位数的中位数 0.980，最差 split 0.945），但测试 q 仍有长尾，这与校准尺度/规范不一致相符。下一项已经冻结但尚未得到正式结果的实验，是只用 meta-fit 电池前缀内部验证在 `{0, 0.001, 0.01, 0.1, 1}` 中选择一个 soft q-prior 强度，再原样应用到 structure-validation 电池；正式结果出来前，不改变 Stage C 的 FAIL，也不开始符号 Stage C2。

完整的逐单元结果、门槛和可读报告分别保存在 `runs/nasa_support_matched_q_diagnostic_20260826/`、`runs/nasa_convex_support_q_diagnostic_20260826/`、`runs/nasa_support_box_q_diagnostic_20260826/` 和 `runs/nasa_prefix_q_training_pilot_20260826/`。这些实验都来自已反复暴露的 inner development cohort，只能用于机制选择；最终论文确认仍需要新隔离电池或外部数据。

### 4.1 历史主实验总体预测（部分数据已失效）

4.1--4.3 节保留旧实验的完整审计轨迹，但其中旧 NASA 和被警告的 Starry 数据不能再作为论文证据；NASA 的当前结论只读 4.0 节。

| 方法 | 均值 NRMSE | 中位 NRMSE |
|---|---:|---:|
| support kNN | **0.1996** | **0.0761** |
| `alternating_mse` | 0.2030 | 0.1497 |
| `joint_mse` | 0.2165 | 0.1425 |
| `alternating_fixed` | 0.2565 | 0.1909 |
| `joint_fixed` | 0.2635 | 0.1773 |
| `joint_dynamic` | 0.2808 | 0.1848 |
| `alternating_dynamic` | 0.2817 | 0.1707 |
| Random Forest | 0.5835 | 0.5814 |
| no-q MLP | 0.5901 | 0.5540 |

在论文范围数据中，support kNN 的总体均值和中位数都最好，但逐数据集结果仍有明显异质性，不能只用汇总数替代任务级表格。

### 4.2 历史核心配对比较（部分数据已失效）

| 对比 | 中位相对效应 | 目标方法胜率 | Wilcoxon / BH | 判断 |
|---|---:|---:|---:|---|
| `joint_mse` vs no-q MLP | `joint_mse` 改善 71.76% | 33/35 | p=1.46e-9，BH q=1.16e-8 | 强证据 |
| `joint_mse` vs Random Forest | `joint_mse` 改善 67.66% | 31/35 | p=6.40e-9，BH q=2.56e-8 | 强证据 |
| `joint_mse` vs support kNN | `joint_mse` 相对恶化 116.56% | 7/35 | p=0.0287，BH q=0.0287 | kNN 在该范围整体更强 |
| `alternating_mse` vs `joint_mse` | 原始差中位 -0.00736 | 22/35 | p=0.0287，BH q=0.0287 | 小幅优势，仍需独立确认 |

正则组合相对 `joint_mse` 的预测显著更差：

- `joint_fixed`：原始 NRMSE 中位增加 0.04585，p=0.00702，BH q=0.0139。
- `alternating_fixed`：增加 0.04793，p=0.0107，BH q=0.0142。
- `joint_dynamic`：增加 0.01896，p=0.00630，BH q=0.0139。
- `alternating_dynamic`：增加 0.03196，p=0.00868，BH q=0.0139。

### 4.3 历史逐数据集结果（部分数据已失效）

下表覆盖论文范围的 **7 个真实数据集 × 全部 9 种方法**。单元格均为 seeds 0–4 的 `reference_nrmse` **中位数 [Q1, Q3]**（每格 n=5），越低越好。`†` 表示该数据集上最好的潜变量方法，粗体表示跨全部方法的全局最佳；加粗仅作描述，显著性判断以 4.2 节的配对检验为准。数据来自 `real_all_runs.csv` 的论文范围子集，没有挑选种子。

#### 潜变量方法

| 数据集 | J-MSE | A-MSE | J-Fixed | A-Fixed | J-Dynamic | A-Dynamic |
|---|---:|---:|---:|---:|---:|---:|
| MATR battery | 0.1299 [0.1261, 0.1397] | 0.1360 [0.1062, 0.1397] | 0.1397 [0.1377, 0.1507] | 0.1292 [0.1262, 0.1426]† | 0.1387 [0.1288, 0.1488] | 0.1452 [0.1250, 0.1513] |
| NASA battery（旧协议，已失效） | 0.4761 [0.4132, 0.7397]† | 0.5130 [0.4762, 0.6176] | 0.5845 [0.5424, 0.6225] | 0.4982 [0.4928, 0.5398] | 0.7173 [0.6434, 0.7653] | 0.7165 [0.7075, 0.7778] |
| Starry electrical | 0.1421 [0.1052, 0.1986] | 0.1097 [0.1094, 0.1530]† | 0.1452 [0.1231, 0.1903] | 0.1278 [0.1271, 0.1411] | 0.1426 [0.1414, 0.1753] | 0.1503 [0.1386, 0.1517] |
| Starry seebeck | 0.1041 [0.0860, 0.1094] | 0.0682 [0.0659, 0.0815]† | 0.3047 [0.2818, 0.3250] | 0.2948 [0.2534, 0.3378] | 0.3640 [0.2762, 0.3738] | 0.3064 [0.2818, 0.3251] |
| Starry thermal | 0.0996 [0.0958, 0.1189]† | 0.1132 [0.1115, 0.1277] | 0.1688 [0.1455, 0.1711] | 0.1756 [0.1738, 0.1772] | 0.1412 [0.1378, 0.1848] | 0.1637 [0.1506, 0.1693] |
| Starry zT | 0.2994 [0.2896, 0.3025] | 0.2781 [0.2315, 0.2874]† | 0.3008 [0.2787, 0.3205] | 0.3157 [0.2821, 0.3448] | 0.3242 [0.2910, 0.3335] | 0.3207 [0.2239, 0.3229] |
| UCI gas drift | 0.1650 [0.1639, 0.1717]† | 0.1719 [0.1633, 0.1733] | 0.1716 [0.1655, 0.1755] | 0.1768 [0.1750, 0.1783] | 0.1747 [0.1626, 0.1752] | 0.1707 [0.1684, 0.1747] |

缩写：`J` = joint，`A` = alternating。

#### 基线方法

| 数据集 | support kNN | Random Forest | no-q MLP |
|---|---:|---:|---:|
| MATR battery | **0.0600 [0.0593, 0.0618]** | 0.5825 [0.5809, 0.5826] | 0.6890 [0.6807, 0.7013] |
| NASA battery（旧协议，已失效） | **0.3044 [0.3020, 0.3634]** | 1.113 [1.112, 1.132] | 0.8268 [0.8188, 0.8619] |
| Starry electrical | **0.0579 [0.0453, 0.0606]** | 0.6142 [0.6069, 0.6237] | 0.5540 [0.4955, 0.5858] |
| Starry seebeck | **0.0293 [0.0290, 0.0294]** | 0.2373 [0.2350, 0.2387] | 0.4735 [0.3447, 0.4986] |
| Starry thermal | **0.0838 [0.0810, 0.0873]** | 0.4708 [0.4649, 0.4766] | 0.4160 [0.3537, 0.4203] |
| Starry zT | **0.0698 [0.0658, 0.0777]** | 0.9176 [0.9082, 0.9185] | 0.9931 [0.9398, 1.162] |
| UCI gas drift | 0.7579 [0.7284, 0.7746] | **0.1625 [0.1516, 0.1641]** | 0.1737 [0.1736, 0.1787] |

逐数据集结果表明：按五种子 NRMSE 中位数，7 个论文范围真实数据集的全局最佳都来自 support kNN 或 Random Forest。support kNN 在六个数据集上最好，RF 在 UCI gas drift 上最好；潜变量方法没有在任一数据集上取得最低中位数。潜变量方法中，MSE-only 在 6/7 个数据集上取得潜变量内部最低中位数，只有 MATR 的正则版本略低，但成套正则的总体预测仍显著退化。

**结论：** 真实数据不支持“潜变量方法在每个任务上都是 SOTA”。它支持的是：潜变量 MSE 模型显著优于支持盲模型，并提供比 support kNN 更平滑的跨任务鲁棒性；特定数据集仍应选 kNN 或 RF。这进一步说明 direct support-conditioned no-q 学习基线是下一轮不可缺少的比较。

## 5. 哪些设计选择会改变结果

这一节不再比较完全不同的方法，而是回答三个工程问题：q 应该包含几个数字、给新实体多少已知点、以及共享网络和 q 应该一起更新还是交替更新。

### 5.1 q 维数

公平比较使用相同的 7 个论文范围数据集、seeds 0–2、MSE-only 模型：

| 方法 | q=1 均值/中位 | q=2 均值/中位 | q=4 均值/中位 |
|---|---:|---:|---:|
| joint | 0.2841 / 0.1873 | 0.2017 / 0.1421 | **0.1660 / 0.1156** |
| alternating | 0.2473 / 0.1696 | 0.2013 / 0.1360 | **0.1458 / 0.1039** |

配对结果：

- joint q=1 相对 q=2 中位恶化 31.82%，仅 1/21 胜，p=6.68e-6。
- alternating q=1 相对 q=2 恶化 24.73%，4/21 胜，p=0.0101。
- joint q=4 相对 q=2 改善 18.60%，13/21 胜，p=0.0888，属于提示性证据。
- alternating q=4 相对 q=2 改善 23.60%，17/21 胜，p=0.00488。

**决策：** q=1 不适合作为真实数据默认值；q=4 是预测优先默认，q=2 可作为更低成本、更强瓶颈的折中。

### 5.2 支持集比例

| 方法 | support=0.1 | support=0.3 | support=0.5 |
|---|---:|---:|---:|
| joint 均值/中位 | 0.1615 / 0.1314 | 0.1614 / 0.1299 | 0.1503 / 0.1270 |
| alternating 均值/中位 | 0.1737 / 0.1182 | 0.1617 / 0.1097 | 0.1594 / 0.1072 |

- 0.1 相对 0.3 明显退化：joint p=0.0130，alternating p=0.000236。
- 0.5 相对 0.3 仅有约 1% 的中位改善；joint p=0.0385，alternating p=0.170。

**决策：** 0.3 是合理的效益/成本中心条件；0.1 可作为少样本压力测试；0.5 的边际收益有限。

### 5.3 联合与交替

在 153 个真实数据配对块中，alternating 相对 joint：

- NRMSE 均值差 -0.00568、中位差 -0.00585，胜率 59.5%；
- continuity 中位差 -0.0050，未改善；
- trustworthiness 均值差 -0.0105；
- distance Spearman 均值差 +0.0048；
- 局部失真没有稳定优势。

所以 alternating 可能有小幅预测收益，但不是连续性或几何质量的通用改进。

## 6. q 是否连续、稳定并具有物理含义

预测准确只说明 q 对任务有用，不说明它形成了合理的“状态地图”。例如，模型可能把两个响应非常相似的电池放在 q 空间的两端，也可能换一个随机初始化后把整张地图扭曲。以下指标用于区分“能预测”与“表示有结构”。

### 6.1 连续性到底衡量什么

本项目的连续性指标回答：**响应相似的实体，在 q 空间是否仍是邻居，且邻域关系在不同尺度下是否平滑保持？**

应联合报告：

- continuity AUC / trustworthiness：邻域保持；
- response-distance Spearman：全局距离排序；
- local log-distortion p95：局部拉伸或压缩；
- 跨种子对齐 q NRMSE 和 kNN overlap：训练复现性；
- 合成 true-q alignment/CCA：与已知生成因子的关系；
- 预测 NRMSE：表征是否真的有任务效用。

单独提高 continuity 可能来自塌缩；单独降低预测误差也可能使用非物理、弯曲的坐标。因此没有任何一个指标可以单独证明“q 连续且物理可解释”。

### 6.2 跨种子稳定性

合成数据中，MSE-only 方案的对齐稳定性较好：

| 方法 | aligned seed NRMSE 中位 | kNN overlap 中位 |
|---|---:|---:|
| `alternating_mse` | 0.460 | 0.184 |
| `joint_mse` | 0.507 | 0.197 |
| fixed/dynamic | 0.617–0.876 | 0.213–0.250 |

真实数据中正则组合提高了部分跨种子几何稳定性：

| 方法 | 经旋转/缩放对齐后的 seed NRMSE 中位 | kNN overlap 中位 |
|---|---:|---:|
| `alternating_fixed` | 0.582 | 0.475 |
| `joint_fixed` | 0.630 | 0.480 |
| `joint_mse` | 0.854 | 0.425 |
| `alternating_mse` | 0.884 | 0.420 |

但相同正则在 153 个真实配对块中使预测 NRMSE 中位恶化约 0.009–0.019，并使局部失真中位增加约 0.39–0.62。因此这里存在真实的**稳定性—精度—局部几何**权衡，而不是正则化净胜。

### 6.3 能否声称 q 是物理变量

不能。当前最多可以说：

- q 是预测上有用、与响应几何有统计关联的实体描述符；
- 在已知真因子的合成任务上，可以评价其对齐程度；
- 即使预测近乎完美，q 仍可能是非线性重参数化、过完备嵌入或多个物理因素的混合；
- 真实数据中的采集协议、工况、时间和设备差异也可能进入 q。

要提升物理可识别性，需要干预、辅助变量、已知守恒/对称结构、解耦监督或跨环境不变性，而不只是连续性损失。

## 7. 测试时如何更可靠地找到新实体的 q

测试时共享网络已经固定，剩下的问题是在一个非凸误差面上寻找新实体的 q。单一起点可能落入较差解；K4 校准的直观做法是从四个候选起点分别搜索，再用 support 内部保留的一小部分数据选择起点，最后在条件允许时精修。

### 7.1 最终推荐配置

当 `q_dim >= 2` 且预测优先时：

- 从训练 q 的经验先验初始化；
- 4 个起点；
- 每起点 200 个 Adam 步，学习率 0.05；
- 支持集内部 25% 用于起点选择；
- 仅在支持行数至少 24 时启用内部切分；
- 仅在发生内部切分时，对选中起点再做 50 步全支持集精修。

q=1 或延迟极敏感场景保留原始单起点校准。

### 7.2 六种子真实数据确认

在 3 个真实 q=4 数据集、joint/alternating、6 个种子的 36 个配对块上，相对原始单起点校准：

| 指标 | 配对结果 |
|---|---:|
| NRMSE | 中位 -8.29%，95% bootstrap CI [-14.67%, -5.47%] |
| 胜率 | 34/36（94.4%） |
| Wilcoxon p | 1.30e-6 |
| 每标签 p95 NRMSE | 中位 -5.38%，95% CI [-13.03%, -2.28%] |
| response continuity AUC | 中位恶化 3.11%，p=0.091 |
| local log-distortion p95 | 中位改善 0.85%，p=0.636 |
| 校准时间 | 均值 4.00×，配对中位比 4.06× |

分数据集结果如下；每个数据集包含 joint/alternating × 6 seeds，共 12 个配对块：

| 数据集 | NRMSE 中位相对变化 | 胜率 | Wilcoxon p | 判断 |
|---|---:|---:|---:|---|
| Starry thermoelectrics | -18.72% | 12/12 | 0.000488 | 强且一致 |
| MATR battery | -11.59% | 12/12 | 0.000488 | 强且一致 |
| NASA battery | -0.59% | 10/12 | 0.07715 | 效应很小、未确认 |

未参与阈值开发的 seeds 3–5 单独仍保持 7.25% 中位改善、88.9% 胜率、p=0.0104。min-32 比 min-24 少改善 1.02 个百分点，超过预先设定的 1 点容忍度，因此冻结 min-24。

### 7.3 合成机制验证和失败审计

- expression 3：+0.01%，基本中性；
- expression 41：-0.17%，轻微改善；
- expression 48：-11.75%，6/6 胜，同时 true-q aligned NRMSE 改善 6.40%，continuity AUC 改善 2.43%。

早期 adaptive24 尝试被正式判为无效并保留：18 行支持集被再次切分后数据过少，使简单任务误差增加约 208%；在未发生内部切分时仍执行高学习率 50 步精修，又把已收敛 q 推离最优点。后续加入“支持集至少 24 行才内部切分”和“只有发生切分才精修”两条规则，作废运行不进入任何聚合结论。

**研究含义：** 当前主要失败不是解码器容量，而是测试时非凸 q 优化的起点、选择和过度精修。校准改进比继续堆叠训练正则更有确定性。

## 8. 精确 Burgers 方程：预测好不等于找回物理参数

Burgers 方程是描述非线性输运和黏性扩散的经典偏微分方程（PDE）。这里使用它的一个已知精确冲击波解，并把振幅和黏度设为两个真实隐藏参数。由于方程残差、参数和数据划分都可精确检查，这组实验专门用于研究“预测性 q”与“物理可解释 q”是否一致。

### 8.1 设置有效性

使用一维黏性 Burgers 方程的精确 traveling-shock 族：

\[
u_t+u u_x=\nu u_{xx},\quad
u=c-a\tanh\left(\frac{a(x-ct-x_0)}{2\nu}\right).
\]

隐藏真状态为二维 `(amplitude a, log viscosity log(nu))`，训练、验证和测试参数标签互不重叠；解析 PDE 最大残差为 `4.44e-16`。

### 8.2 结果

在真维数 q=2：

- adaptive latent 模型中位 NRMSE = **0.01067**；
- 把所有实体混合训练的 no-latent MLP = **0.29954**，潜变量降低 96.5%；
- 输入真实参数的有限容量 MLP = **0.01373**。

| q 维数 | 预测 NRMSE | true-q aligned NRMSE | continuity AUC | 解读 |
|---:|---:|---:|---:|---|
| 1 | 0.05702 | 0.90322 | 0.77455 | 一个预测方向不足以恢复两个真因子 |
| 2 | **0.01067** | 0.91248 | 0.81796 | 预测最好，但坐标不是物理参数的简单仿射变换 |
| 4 | 0.01239 | **0.39743** | **0.89959** | 预测略差，物理嵌入明显更好 |

q=4 相对 q=2 预测恶化 17.6%，但 aligned true-q 恢复改善 56.2%，continuity 增加 0.053。这说明“两个真实参数，所以 q 也应恰好是二维”并不保证学到的两个坐标就是那两个物理参数。模型可以用弯曲、旋转或混合后的坐标完成预测；额外维数则给它更多空间保存物理邻域。当前存在一个无法同时最优的取舍：

- **预测优先：** q=2 + joint；
- **几何/解释优先：** q=4 + alternating。

q=4 alternating 的六种子 adaptive K4 相对原始单起点校准：预测中位改善 2.68%（3/6 胜），p95 改善 10.94%（6/6 胜），aligned NRMSE 改善 43.11%（5/6 胜），continuity 提高 2.12%（4/6 胜）。仍有一个明显对齐离群种子，所以物理可识别性尚未解决。

### 8.3 正则化否定结果

- whitening `1e-3`、`1e-2` 同时损害预测和几何，拒绝；
- smoothness `1e-4` 未支配无正则 adaptive；
- Jacobian disentanglement `1e-4` 在 seeds 0–2 初筛看似有利，但 seeds 3–5 确认时预测恶化 4.6%、aligned NRMSE 恶化 35.1%，仅 1/3 预测块获胜，拒绝推广。

这是一个重要的阴性结果：**几何正则在开发种子上的好看提升，可能无法跨种子复现。**

## 9. 外部 PDEBench 状态

PDEBench 是公开的数值 PDE 基准。PDEBench Burgers `Nu=0.02` 官方文件（shape `(10000, 201, 1024)`）已经完成完整性验证、正式训练和冻结分析。核心比较使用 10 个配对种子、support 比例 0.3；下表为 reference NRMSE 中位数，越低越好。

| 方法 | q 维数 | NRMSE 中位数 | 相对解释 |
|---|---:|---:|---|
| latent joint + adaptive K4 | 4 | 0.2967 | 小 q 潜变量 |
| latent joint + adaptive K4 | 8 | 0.2750 | 提高容量后改善 |
| latent joint + adaptive K4 | 16 | **0.2499** | 当前潜变量最佳 |
| latent alternating + adaptive K4 | 16 | 0.2555 | 与 joint 接近但略差 |
| latent q=16 continuity，1000-epoch matched | 16 | 0.2573 | 新函数基线的严格配对 q anchor |
| FPCA + support ridge | — | 0.2753 | 经典线性函数空间；10/10 不及 q=16 continuity |
| masked DeepONet | — | 0.3136 | 信息匹配 learned operator；10/10 不及 q=16 continuity |
| support mean | — | 0.3502 | 只输出 support 平均响应 |
| pooled no-latent MLP | — | 0.9488 | 不建模新轨迹状态，明显更差 |
| full-IC PCA MLP reference | — | 1.2718 | 在当前信息/容量匹配下也较差 |
| support kNN-4 | — | **0.2113** | 全表最佳，仍优于 q=16 |

q=16 joint adaptive 在 10/10 个配对种子中都优于 pooled MLP 和 support mean，并在 10/10 中优于 q=16 的 legacy 单起点校准；相对 q=8 adaptive 也在 9/10 个种子中改善。这证明显式 q 与多起点校准在公共 PDE 数据上具有实质预测价值。

新增的同-query 函数基线显示，匹配更新的 q=16 continuity 在 10/10 个种子中优于 FPCA 和 masked DeepONet；但 support kNN-4 同样在 10/10 个种子中优于 q=16 latent。因此最诚实的结论是：**q 有用而且对已声明函数基线有竞争力，但尚未成为 support-aware 方法中的 SOTA。** 另外，q 从 4 增至 16 时预测变好、响应连续性却总体下降，再次说明预测最优表示不必是几何最平滑表示。PDEBench 仍是数值模拟，不能替代实验室或现场真实数据。

## 10. 对“哪个方法更有意义、效果更好”的最终回答

| 目标 | 当前首选 | 原因 | 证据等级 |
|---|---|---|---|
| 真实数据预测默认 | q=4、MSE-only、adaptive K4；joint/alternating 都保留 | q4 预测最好；K4 有独立种子确认；训练日程无稳定胜负 | 已证实/较强 |
| 低计算成本 | q=2、原始单起点或较少起点校准 | q2 成本低，性能仍显著优于支持盲基线 | 提示性 |
| 合成预测 | `joint_mse` + 合理校准 | 对 no-q/RF 强优势；整体比 alternating 稳 | 已证实 |
| PDE 预测 | exact Burgers 用 q=2；PDEBench 当前用 q=16 + joint adaptive | 自建机理任务偏好强瓶颈，公共高维轨迹任务偏好更大 q | 机理级 + 公共数值基准证据 |
| PDE 几何解释 | q=4 + alternating + adaptive K4 | true-q 对齐与连续性更好 | 机理级、非唯一识别 |
| 稳定 q 坐标 | fixed 正则可作研究对照 | 跨种子稳定性变好，但预测和局部失真变差 | 不推广 |
| 单个真实任务追求最低误差 | 先交叉验证 support kNN / RF | 多个数据集上简单基线更强 | 已证实 |

真正最有研究意义的不是某个 fixed/dynamic 分支，而是：

1. **显式 q 瓶颈是否比一般 support conditioning 更有益；**
2. **如何用支持编码器直接给出 q 初值，再用少量梯度精修消除校准失败；**
3. **如何在预测、连续性、稳定性和物理可识别性之间给出可验证的取舍边界；**
4. **如何在外部 PDE/ODE 和真实科学系统上验证，而不依赖单一合成族。**

### 10.1 外部读者最容易提出的三个问题

**既然 kNN 或 RF 在每个真实数据集上都能找到一个赢家，为什么还研究 q？** 逐数据集选择不同基线与学习一个可跨实体、可插值、可用于后续分析的共享状态表示不是同一个目标。q 可能提供跨任务建模、状态跟踪和物理分析价值，但目前必须先承认它在纯预测上没有击败“每个数据集事后挑选的最佳简单方法”。

**测试时已经看到了 support 的 y，为什么还叫潜变量？** 因为 q 本身没有被直接测量或给定，而是由 support 响应间接推断。它类似从少量体检指标估计健康状态：观测是已知的，状态仍是潜在的。

**如果真实数据没有真 q，怎么知道学得好不好？** 只能从多个侧面间接判断：预测是否准确、响应相似实体是否相邻、不同种子是否稳定、增加或减少 support 后是否合理变化。只有在合成或已知物理参数实验中，才能直接评价 q 与真因子的对应。

## 11. 已补齐的关键基线与仍缺的比较

目前已经完成四类关键开发性比较：

1. **DeepSets / attentive CNP：** 无显式 q、直接使用 support 的 learned baseline 已运行。全局平均 DeepSets 偏弱；query-to-support attention 只在 battery 相对 DeepSets 明显改善，并没有跨数据集胜出。
2. **支持编码器 + q 精修/额外候选：** 编码器能改善 q 的响应连续性，但未稳定改善预测；把编码器放入 multistart 候选池后也几乎复现原 joint 结果，没有解决共享 decoder 的 Starry 尾部。
3. **support-relative bounded CNP 与 reliability selector：** 显式输出边界消除了 Starry 灾难外推；support 内部路由相对 attentive CNP 改善 battery，但仍不及该任务上的 q 和 kNN，尚未形成跨任务赢家。
4. **FPCA 与 masked DeepONet：** PDEBench 上已完成 10-seed、同 support/query 的经典线性函数空间和信息匹配 operator 比较。q=16 continuity 胜过两者，但仍不及 support kNN。

仍值得补、但不再是当前首要阻断的比较：

1. **Gaussian Process 或 sparse GP：** 可补充不确定性视角，但已有 FPCA 作为经典函数基线；
2. **FNO：** 只有在论文要提出广泛 operator-learning 优势时才是硬需求；当前 masked DeepONet 已覆盖信息匹配的 learned operator 对照；
3. **冻结鲁棒候选的独立 seed/数据集确认：** 仍是比继续堆基线更重要的缺口。

因此，现在的缺口已经从“缺少关键 baseline”变成“已有较完整基线，但尚无一个同时解决 Starry 尾部并保持 battery 精度、且通过独立确认的主方法”。

## 12. 下一阶段实验优先级

### P0：先冻结可复现版本

- 把当前研究代码提交到专用分支并打 tag；
- 保存完整 `git diff`、依赖锁、CUDA/PyTorch 和数据清单；
- 从干净 worktree 重跑最终确认矩阵；
- 预先冻结主终点、种子和晋级门槛。

### P1：实现最关键的公平比较

- 保留已完成的 DeepSets/CNP、q 编码器和 bounded/selector 开发结果；
- 用训练实体或独立开发集学习保守的 selector margin，不能在当前开发测试单元上事后调阈值；
- 保留已完成的 FPCA/DeepONet；是否再补 GP/FNO 取决于最终论文主张，不再优先于主方法确认；
- 冻结最多一个候选，再用新 seeds/数据集确认。

主终点用配对 NRMSE；同时报告 p95、灾难失败率、校准时间、q 稳定性和连续性。当前编码器路线没有保持 K4 精度，不能晋级；新的价值点是能否用预先学习的可靠性规则同时保留 battery 精度与 Starry 安全性。

### P2：完整真实数据确认

当前广泛真实实验对每标签做了行数上限，是筛选性基准实验。最终需要在全行数据或预先固定的分层子样本上，使用 5 个种子确认最多两种新方法，并保留 kNN/RF 的任务级结果。

### P3：外部物理系统

PDEBench Burgers 核心、support-ratio、FPCA 和信息匹配 masked DeepONet 都已完成。下一步不再重复同类 q 或 DeepONet 扫描；只有在主张 operator-learning 普适优势时才补 FNO，否则优先真实数据鲁棒候选的冻结确认。

### P4：更远领域

优先顺序仍是：参数化 PDE/ODE 与逆问题 > 电池/材料/设备数字孪生 > 机器人少样本系统辨识 > 不规则生理时间序列 > few-view inverse graphics。视觉适合做迁移研究，但不应先于关键 support-conditioned 基线。

## 13. 统计有效性与 11 项谬误审计

这一节回答“数字可能怎样误导我们”。`CAUTION` 表示必须限制表述或补充验证，不等于结果错误；`NOTE` 表示已记录但当前不构成主要阻断；“不适用”表示该风险与本实验类型无直接关系。

| 风险 | 审计结果 | 处理 |
|---|---|---|
| 1. Simpson 悖论 | **CAUTION** | 总体方向会掩盖 expression 15/21、Starry 与 battery 等异质性；始终保留分任务和任务-种子内配对 |
| 2. 生态谬误 | **CAUTION** | 数据集级连续性不能推出每个实体的 q 是物理状态 |
| 3. Berkson 偏差 | **CAUTION** | 真实数据有标签筛选和每标签上限，样本进入基准实验的机制可能影响泛化 |
| 4. Collider 偏差 | **未发现直接问题** | 没有通过后验协变量调整提出因果结论；仍不作因果解释 |
| 5. 基率忽视 | **不适用** | 主要终点为连续回归，不是罕见事件分类 |
| 6. 回归均值 | **不适用** | 没有按极端基线值入组再评价回落 |
| 7. 幸存者偏差 | **CAUTION** | 正式矩阵有完成性记录、无效运行保留；但发布前仍需对 launcher return codes 做全量清点 |
| 8. 多重比较/寻找显著 | **CAUTION** | 主方法比较使用 BH；部分敏感性和校准表为未校正 p 值，应把效应量、CI 和未见种子确认置于 p 值之前 |
| 9. 分叉分析路径 | **CAUTION** | 多轮开发具有迭代性；阈值和 Jacobian 有独立种子确认，但整个研究不是最初一次性预注册 |
| 10. 相关冒充因果 | **CAUTION** | q-response 连续性只说明关联和预测效用，不说明物理因果恢复 |
| 11. 反向因果 | **NOTE** | q 本来就是从支持响应推断；不能反过来声称 q 是响应的唯一生成原因 |

**总体统计置信等级：CAUTION。**

- 对“支持条件化显著优于支持盲模型”和“min-24 K4 改善指定 q4 真实任务预测”的置信度高。
- 对“某一训练日程普遍更优”“正则能改善 q”“显式 q 优于所有支持感知模型”“q 是唯一物理状态”的置信度不足。

## 14. 可复现性审计

### 14.1 已具备

- 正式运行包含 manifest、原子 `result.json`、checkpoint、逐策略预测和 q 值；
- 校准无效运行保留 `INVALIDATED.md`，没有静默并入；
- 精确 Burgers 保存解析残差和环境信息；
- 主分析基于配对块、胜率、效应量、Wilcoxon 和必要处的 BH；
- 相关代码改动后已有 44 项 pipeline/protocol/support-conditioned 单元测试通过记录。

### 14.2 发布阻断项

当前位于本地分支 `research/latent-q-extended-20260810`，工作树仍包含多项 tracked 修改和未跟踪研究脚本；远端只有 `origin/main`，基础提交仍为 `2b13869`。这意味着结果对应的精确代码状态尚未被不可变地标识。

简单说，实验文件虽然保存完整，但“究竟是哪一版代码生成了这些结果”目前还不能只靠一个提交编号还原。这不会自动推翻结果，却会阻止第三方严格复现，也会增加后续数字与代码错配的风险。

因此本报告是 **ANALYZED**，不是最终 **VERIFIED/REPRODUCED**。在投稿或公开结果前必须：

1. 提交并 tag 当前代码；
2. 记录数据校验和和环境锁；
3. 从干净版本重跑最终确认集；
4. 用独立脚本从原始 `result.json` 一键重建表格和图；
5. 核对所有声明的数字与原始文件逐项一致。

## 15. 最终研究决策

### 推广

- q_dim≥2 的 min-24 adaptive K4 测试时校准；
- q=4 作为真实预测默认、q=2 作为低成本或强瓶颈对照；
- MSE-only joint/alternating 作为主方法双基线；
- 预测、尾部误差、连续性、失真和跨种子稳定性的联合评价。

### 保留为对照，不推广

- fixed/dynamic 成套正则；
- whitening、smoothness、Jacobian 当前权重；
- q=1 真实数据默认；
- 把 finite true-q MLP 称作 oracle。

### 明确禁止的过度结论

- “潜变量模型在所有真实数据上最好”；
- “q 已恢复真实物理参数”；
- “continuity 提升就代表 latent 更好”；
- “显式 q 已证明优于所有支持集方法”；
- “PDEBench 已证明本方法普遍优于所有 support-aware 或神经算子方法”。

## 16. 证据索引

- 合成/真实主实验与图：`runs/iclr_discovery_analysis_20260808/`
- 真实 q 维数、支持比例、日程、正则敏感性：`runs/iclr_real_sensitivity_analysis_20260808/`
- 校准研究总报告：`runs/iclr_calibration_research_20260809/README.md`
- q4 六种子阈值配对结果：`runs/iclr_calibration_threshold_combined_q4_seeds0to5_20260809/`
- 作废校准运行审计：`runs/iclr_synthetic_calibration_adaptive24_20260809/INVALIDATED.md`
- 精确 Burgers 总报告：`runs/burgers_latent_research_20260809/README.md`
- PDEBench 预备协议：`runs/pdebench_burgers_latent_20260809/PROTOCOL.md`
- PDEBench 与扩展 15h 完整分析：`runs/extended_15h_analysis_20260810/EXTENDED_15H_RESULTS.md`
- 1000-epoch 真实/PDE 公平性终态分析：`runs/matched_1000ep_real_pde_20260817/analysis/MATCHED_1000EP_REAL_PDE_RESULTS.md`
- PDEBench FPCA/DeepONet 冻结计划：`PDEBENCH_FUNCTIONAL_BASELINES_PLAN_20260822.md`
- PDEBench FPCA/DeepONet 终态分析：`runs/pdebench_functional_baselines_20260822/PDEBENCH_FUNCTIONAL_BASELINE_RESULTS.md`
- support encoder / DeepSets pilot：`runs/iclr_support_encoder_pilot_20260811/SUPPORT_ENCODER_PILOT_RESULTS.md`
- attentive CNP 与 encoder multistart：`runs/iclr_support_followup_20260811/SUPPORT_FOLLOWUP_RESULTS.md`
- support-relative robustness：`runs/iclr_support_robustness_20260811/SUPPORT_ROBUSTNESS_RESULTS.md`
- attentive reliability selector：`runs/iclr_attentive_selector_20260811/ATTENTIVE_SELECTOR_RESULTS.md`
- 下一阶段完整计划：`NEXT_ROUND_RESEARCH_AND_EXPERIMENT_PLAN_20260809.md`

## 17. 最短版汇报口径

这项工作研究如何用一个新科学对象的少量观测，推断它的低维状态 q，并预测剩余响应。对象可以是一块电池、一个材料样品或一组 PDE 参数。

实验已经证明，使用实体 support 信息明显优于完全忽略实体差异的全局模型：潜变量方法相对 no-q MLP 的配对中位误差在合成和真实数据上分别降低约 94.4% 和 69.8%。测试时尝试多个 q 起点还能在指定真实任务上额外降低 8.29% 中位误差。

当前证据没有证明潜变量模型在每个数据集上最好。support kNN 和 Random Forest 在不同真实数据集上取得了更低的五种子中位误差；同时，准确预测也不代表 q 已经恢复真实物理变量。精确 Burgers 实验明确显示，预测最好的 q 维数与物理对齐最好的 q 维数并不相同。

后续补强已经加入 DeepSets/CNP、q 编码器、PDEBench、bounded safety head、support 内部可靠性选择器、FPCA 和信息匹配 masked DeepONet。最重要的新发现是：Starry 的灾难外推可以被 support-relative 有界输出稳定消除，但同一边界会损害 battery；简单 support 内部选择能保留 Starry 安全并改善 battery 的 attentive CNP，却仍不及该任务上的 q 和 kNN，因此没有形成一般赢家。1000-epoch 匹配更新确认还排除了“神经模型只是训练轮数不足”这一简单解释；PDEBench 上 q=16 continuity 又在 10/10 seeds 中胜过 FPCA 和 masked DeepONet，但 kNN 仍是最强同-support 方法。

## 18. 2026-08-11 support-conditioned 补强实验更新

这一节是后续更新，使用论文范围内统一的 2 数据集 × 3 seed 开发矩阵。它解决两个外部读者会直接追问的问题：一是“无显式 q 的 learned support 模型表现怎样”，二是“Starry 的巨大失败究竟能不能修”。数值均为 reference-scaled NRMSE 中位数 `[最小, 最大]`，越低越好；Starry 的范围必须保留，因为只报中位数会掩盖灾难 seed。

### 18.1 每个数据集的完整对比

| 方法 | NASA battery | Starry Seebeck |
|---|---:|---:|
| joint continuity q | **0.2180** [0.2005, 0.2313] | 0.0304 [0.0055, 28976.6] |
| attentive CNP | 0.4619 [0.4387, 0.5954] | 4947 [0.1727, 78674.2] |
| bounded support-relative CNP | 0.9015 [0.2510, 0.9358] | 0.003595 [0.003510, 0.003989] |
| support-internal reliability selector | 0.3627 [0.2525, 0.4421] | 0.003595 [0.003508, 0.003634] |
| support kNN | 0.3044 [0.3020, 0.3634] | **0.002178** [0.002110, 0.002287] |
| Random Forest | 1.113 [1.112, 1.132] | 0.01153 [0.01121, 0.01411] |

这里没有一个方法横扫两列。battery 最适合显式 q，Starry 最适合 support kNN。可靠性选择器在 battery 胜过 attentive CNP，并在 Starry 消除全部灾难 seed，但这个两任务开发矩阵不足以证明它能够普遍路由。

### 18.2 Starry 为什么会爆炸，什么真正修好了它

生产协议按固定种子把每个标签最多保留 256 行。精确复现这一 cap 后，训练目标仍含一个 `Bi2Te3=-37.57071` 极端值；训练集标准差为 0.3199，而 capped 测试目标只在 `[-0.01258, 0.02060]`。失败神经网络会对少数未见材料输出近似常数的 $10^4$–$10^5$ 数值，远超任何 support 观测。

递进消融给出清晰因果线索：

| Starry 方案 | NRMSE 中位数 | 灾难 seed (`NRMSE>1`) | 最大标准化输出量级 |
|---|---:|---:|---:|
| support-relative + MSE | 1125 | 3/3 | $1.90\times10^7$ |
| support-relative + Huber | 21.83 | 3/3 | $6.82\times10^5$ |
| support-relative + Huber + tanh bound | **0.003595** | **0/3** | **8** |

所以 Huber 只能减弱极端点影响，不能阻止网络在新组成上外推。真正切断失败尾部的是：以当前实体 support 的中位数/MAD 表示目标，再把预测限制在固定的标准化残差范围。所有统计都只使用 support；query targets 只在最后评分。

### 18.3 为什么还不能直接推广 bounded 方案

同一边界在 battery 会压掉有用的响应幅度：bounded CNP 中位 NRMSE 为 0.9015，而显式 q 和 kNN 分别为 0.2180 与 0.3044。support 内部可靠性选择器把它改善到 0.3627，但仍没有达到该任务上的最佳方法。

这条阴性结果很重要：消除 Starry 爆炸并不自动等于得到可跨数据集采用的主方法。不能在看完这 6 个开发测试单元后临时调 margin 并把结果称为确认；合理下一步是在训练实体或单独 development 数据上学习规则，再一次性冻结到新 seeds/新数据集。

### 18.4 表示连续性没有随安全性自动改善

| 数据集 | attentive CNP continuity | bounded CNP continuity | selector continuity |
|---|---:|---:|---:|
| NASA battery | 0.5980 | 0.4454 | 0.5127 |
| Starry Seebeck | 0.7036 | 0.7120 | 0.6822 |

Starry bounded CNP 虽然预测稳定，但局部 collapse rate 仍约 0.872。也就是说，**防止输出爆炸不等于学到高质量、可分辨的连续表示。** 与之相对，q 编码器路线确实能改善部分 continuity/collapse 指标，却没有带来稳定预测增益；把编码器候选加入 multistart 后，最终预测也与原 joint anchor 几乎相同。这再次确认预测、安全性和表示几何是三个不同目标。

### 18.5 当前补强结论

1. learned support conditioning 已经比较，不再是“完全缺失基线”；但 DeepSets、attentive CNP、q encoder 和 selector 都没有通过跨数据集的一般晋级门槛。
2. bounded support-relative output 是可信的 Starry 失败控制机制，可作为论文中的机制消融和安全讨论。
3. reliability selector 的方向有价值，但还不是冻结主方法；它在 battery 上仍落后显式 q 和 kNN，必须在独立数据上验证。
4. 当前最强、最诚实的论文故事仍是“什么时候显式 latent bottleneck 有用、什么时候局部 support 方法更强、如何测量并控制失败尾部”，而不是单一算法的普遍 SOTA。

## 19. 2026-08-17：1000-epoch 公平性补跑终态

### 19.1 为什么要补跑

此前的关键疑问是：support kNN 不需要 epoch，而神经方法可能因为训练轮数较少而吃亏。为排除这个混杂因素，本轮把神经方法统一训练到 1000 epoch；同一真实数据集内，joint-MSE、joint-continuity 与 no-q MLP 的 backward 次数完全一致。PDE 的所有神经组件也统一为 128,000 次 backward。成功标准是计划任务全部落盘、无失败/超时、结果行数与冻结设计一致，且主指标有限。

终态审计为 **230/230 任务完成，0 失败、0 超时**。其中真实数据为 4 数据集 × 5 方法 × 10 seeds，共 200 个结果；PDEBench 为 30 个训练任务，展开为 100 条策略结果。累计任务槽位时间 10.43 小时，控制器与两个汇总命令均正常退出。

### 19.2 每个真实数据集的结果

下表为 10 个种子的 reference NRMSE 中位数，越低越好。

| 数据集 | joint-MSE q | joint-continuity q | no-q MLP | support kNN | Random Forest |
|---|---:|---:|---:|---:|---:|
| NASA battery | 0.237895 | **0.227871** | 1.36557 | 0.330398 | 1.12406 |
| Starry Seebeck | 219.807 | 0.0126293 | 94773.1 | **0.00219235** | 0.0121824 |
| Starry electrical | 268145 | 280264 | 676158 | **0.505283** | 1.16814 |
| Starry thermal | 361050 | 358259 | 387072 | **0.0563718** | 0.337488 |

NASA battery 是显式 q 的明确正例：continuity q 在 10/10 配对种子上胜过 kNN，MSE q 在 8/10 胜过 kNN。其余三个材料属性上 kNN 均为 10/10 胜出。Seebeck 的 continuity 版本接近 RF，但 electrical 与 thermal 的全部神经方法都出现数量级爆炸。

为避免中位数掩盖失败，下表同时报告 `NRMSE > 10` 的种子数：

| 数据集 | joint-MSE q | joint-continuity q | no-q MLP | support kNN | Random Forest |
|---|---:|---:|---:|---:|---:|
| NASA battery | 0/10 | 0/10 | 0/10 | 0/10 | 0/10 |
| Starry Seebeck | 5/10 | 1/10 | 5/10 | 0/10 | 0/10 |
| Starry electrical | 10/10 | 10/10 | 10/10 | 0/10 | 0/10 |
| Starry thermal | 10/10 | 10/10 | 10/10 | 0/10 | 0/10 |

### 19.3 PDEBench 的公平比较

| 配置 | 10-seed 中位 NRMSE | p90 | continuity | effective rank |
|---|---:|---:|---:|---:|
| q=16 joint-continuity adaptive | 0.257289 | 0.268806 | 0.7759 | 9.171 |
| q=16 joint-MSE adaptive | 0.265060 | 0.269061 | 0.7236 | 10.891 |
| q=8 joint-MSE adaptive | 0.289350 | 0.301779 | 0.7835 | 6.245 |
| support kNN-4 | **0.211295** | 0.218136 | — | — |
| support mean | 0.350155 | 0.352501 | — | — |
| pooled no-q MLP | 0.949249 | 0.951708 | — | — |
| full-IC PCA MLP reference | 1.90629 | 3.78241 | — | — |

q=16 continuity 比 q=16 MSE 在 7/10 配对种子上更好，并同时提高 continuity；这是连续性损失在 PDE 上的正面证据。但两种 q=16 方法都在 10/10 种子上落后 kNN。这里能支持的是“q 相对 support-blind no-q MLP 有价值”，不能支持“q 优于所有 support-aware 方法”。

### 19.4 训练更久是否解决问题

| 任务与方法 | 1000-epoch 中位数 | 较短训练中位数 | 1000-epoch 胜出 seeds |
|---|---:|---:|---:|
| NASA battery joint-MSE | 0.237895 | 0.230819 | 5/10 |
| NASA battery joint-continuity | 0.227871 | 0.220706 | 4/10 |
| Starry Seebeck joint-MSE | 219.807 | 197061 | 7/10 |
| Starry Seebeck joint-continuity | 0.0126293 | 0.0187701 | 7/10 |
| PDE q=16 joint-MSE | 0.265060 | 0.249928 | 0/10 |

长训练改善了 Seebeck continuity 的中位数，并把灾难种子从 2/10 降到 1/10；但它没有给 NASA 带来一致增益，也没有修复另外两个材料属性。PDE q=16 MSE 更是在 10/10 种子上比短训练版本更差。因此，kNN 的领先不能用“神经网络 epoch 没对齐”解释。

### 19.5 本轮改变了什么结论

1. **保留的正结论：** 显式 q 在 NASA battery 明确有效；在 PDEBench 上明显优于 pooled no-q MLP 和 support mean。
2. **新增的负结论：** 1000 epoch 不能普遍修复神经方法，kNN 优势不是简单的训练轮数不公平。
3. **连续性损失的边界：** 它在 PDE 上同时改善预测与邻域保持，在真实数据上却不一致；几何分数、预测误差和数值稳定性必须并列报告。
4. **论文表述边界：** 不能宣称显式 q 普遍优于 support-aware 方法，也不能隐藏 Starry electrical/thermal 的全种子失败。

完整冻结分析、配对效应、优化器更新次数和终态审计见 `runs/matched_1000ep_real_pde_20260817/analysis/`。

## 20. 2026-08-22：PDEBench 经典函数空间与 DeepONet 基线

### 20.1 为什么还要做这一轮

此前 PDEBench 已比较 q、support kNN、support mean 和 pooled no-q MLP，但仍缺两个审稿人会直接追问的对照：经典线性函数空间能否已经足够，以及不显式优化 q 的 learned operator 能否更好利用 support。为此，本轮固定原来的 64/16/32 轨迹划分、16 × 32 网格、seeds 0–9 和 support ratio 0.3，保证每个新方法与旧 kNN/q 使用完全相同的 11,488 个测试 query 行。

- **FPCA + ridge：** 只用训练轨迹拟合函数主成分；测试时只从当前轨迹的 support 估计系数。主成分数和 ridge 系数只在验证轨迹选择。
- **masked DeepONet：** branch 输入 512 位置的 support mask 和 masked support targets，trunk 输入 query 的 `(x,t)`；不输入 query targets。每个 seed 完整执行 128,000 次更新，与已有 q=16 神经块的 backward 上限一致。

终态审计为 **20/20 成功，0 失败、0 超时**。每个结果均有 11,488 个与旧 anchor 逐行一致的 query；把所有 query targets 人为扰动后，两种方法的预测最大变化都严格为 0。

### 20.2 完整主表

下表为 10-seed reference NRMSE 中位数，越低越好。`full-IC` 使用完整初始场，信息更多，只是额外信息参考。

| 方法 | 信息类型 | 中位 NRMSE | p90 | max |
|---|---|---:|---:|---:|
| support kNN-4 | 同 support | **0.211295** | 0.218136 | 0.219243 |
| q=16 joint-continuity adaptive | 同 support | 0.257289 | 0.268806 | 0.270941 |
| q=16 joint-MSE adaptive | 同 support | 0.265060 | 0.269061 | 0.271265 |
| FPCA + support ridge | 同 support | 0.275280 | 0.286818 | 0.291442 |
| q=8 joint-MSE adaptive | 同 support | 0.289350 | 0.301779 | 0.305207 |
| masked DeepONet | 同 support | 0.313649 | 0.318488 | 0.320887 |
| support mean | 同 support | 0.350155 | 0.352501 | 0.353080 |
| pooled no-q MLP | support-blind | 0.949249 | 0.951708 | 0.955234 |
| full-IC PCA MLP | 额外信息 | 1.90629 | 3.78241 | 5.41363 |

### 20.3 严格配对结果

| 候选 | 对照 | 候选胜出 seeds | 中位差（候选−对照） | BH q |
|---|---|---:|---:|---:|
| FPCA | support kNN | 0/10 | +0.06455 | 0.00225 |
| FPCA | q=16 continuity | 0/10 | +0.02186 | 0.00225 |
| FPCA | q=16 MSE | 1/10 | +0.01289 | 0.00586 |
| FPCA | q=8 MSE | 9/10 | -0.01828 | 0.00419 |
| masked DeepONet | support kNN | 0/10 | +0.10282 | 0.00225 |
| masked DeepONet | q=16 continuity | 0/10 | +0.05844 | 0.00225 |
| masked DeepONet | pooled no-q MLP | 10/10 | -0.63342 | 0.00225 |
| FPCA | masked DeepONet | 10/10 | -0.03743 | 0.00225 |

FPCA 在 10/10 seeds 上优于 masked DeepONet，并在 9/10 seeds 上优于 q=8，说明经典线性函数空间是有竞争力的强基线；但 q=16 continuity 在 10/10 seeds 上又优于 FPCA。masked DeepONet 虽然在 10/10 seeds 上优于 pooled no-q MLP 和 support mean，却没有超过 q=8、q=16 或 kNN。

### 20.4 容量和训练并不是解释

masked DeepONet 有 345,217 个训练参数；q=16 continuity 的共享模型加训练实体 embedding 共 11,777 个参数。DeepONet 每个 seed 都完成 128,000 次更新，但验证集在 8/10 seeds 选择 16k checkpoint、2/10 选择 32k，说明更长训练主要带来过拟合而不是弥合差距。因此，这个结果不能解释为 DeepONet 容量或训练轮数不够。

### 20.5 本轮对论文主张的影响

1. **补强：** q=16 不只优于弱 no-q MLP，也优于一个经典 FPCA latent space 和一个信息匹配 masked DeepONet。
2. **不变的边界：** support kNN 仍在 10/10 seeds 上胜过 q、FPCA 和 DeepONet，不能宣称 q 是 PDEBench 上最强的 support-aware 方法。
3. **新的方法学信息：** 在当前稀疏随机 support 协议中，局部插值最有效；全局线性函数空间次之，显式非线性 q 再优于该线性基线，而当前 masked operator 没有发挥参数量优势。
4. **下一步优先级：** 基线缺口已明显缩小。若不把论文写成 operator-SOTA，继续扫 DeepONet/FNO 的价值低于冻结一个能同时控制 Starry 尾部并保持 battery 精度的真实数据主方法。

完整原子结果、配对检验、选择记录与终态审计见 `runs/pdebench_functional_baselines_20260822/`。

## 21. 2026-08-24：q/kNN 训练实体验证门控终态

这一轮没有在测试实体上调规则。每个 seed 先将准备好的训练实体固定分成 75% meta-fit 和 25% meta-validation，只在留出的训练实体上运行 3 个 support/query episodes；开发集 NRMSE 中位数更低的组件被固定为整个数据集的策略。随后 q 用全部训练实体重训，最终策略才用于未见测试实体。冻结矩阵为 NASA battery 和三个 Starry 属性 × seeds 30–39，共 40 个任务。

终态审计为 **40/40 成功、0 失败、0 超时**；40 个任务 ID 唯一，元训练与元验证实体严格不重叠，200 条方法结果齐全，查询目标扰动后的最大预测变化严格为 0。自动汇总最初仅因环境缺少 Pandas 可选的 `tabulate` 包而在报告渲染阶段返回 1；改为无依赖 Markdown 输出后，冻结分析成功重跑，训练结果没有变化。

### 21.1 每个数据集的完整结果

下表为 10-seed reference NRMSE 中位数，括号内为 `NRMSE > 1` 的灾难运行数。query oracle 只用于诊断，不是可部署方法。

| 数据集 | 分层训练实体验证门控 | 纯 q | support kNN | 逐实体 selector | query oracle（不可部署） | q 选择次数 |
|---|---:|---:|---:|---:|---:|---:|
| NASA battery | 0.288962 (0) | **0.237327 (0)** | 0.302517 (0) | 0.271967 (0) | 0.233802 (0) | 8/10 |
| Starry Seebeck | **0.002254 (0)** | 0.004552 (2) | **0.002254 (0)** | 0.002430 (0) | 0.002254 (0) | 0/10 |
| Starry electrical | **0.519762 (0)** | 344547 (10) | **0.519762 (0)** | 0.519734 (0) | 0.519729 (0) | 0/10 |
| Starry thermal | **0.054370 (0)** | 397684 (10) | **0.054370 (0)** | 0.055832 (0) | 0.053731 (0) | 0/10 |

### 21.2 预设门槛与解释

| 门槛 | 结果 |
|---|---|
| 完整性、唯一性、信息边界、零泄漏 | 通过 |
| NASA 至少 8/10 选择 q | 通过，恰好 8/10 |
| NASA 门控中位数不比 q 差超过 5% | **失败：比 q 高 21.76%** |
| 每个 Starry 至少 8/10 选择 kNN、零灾难、在 kNN 5% 内 | 通过，三个数据集均 10/10 选择 kNN并与其数值相同 |
| pooled 不差于逐实体 selector | 通过 |
| 总晋级 | **失败** |

NASA 的问题不是选择 q 的总次数不足，而是选错了具体 seed。门控与测试 query 上真正更优组件只在 6/10 seeds 一致：seeds 32、33 错误选择 kNN，seeds 36、39 错误选择 q。前两个错误用明显更差的 kNN 替换了强 q 结果，从而把中位数从 0.237327 推高至 0.288962。因此，训练实体留出验证能可靠识别 Starry 的灾难外推域，却不足以在 NASA 上稳定路由。

这一 selector 线按冻结规则停止，不在 seeds 30–39 上事后调阈值。论文可保留的结论是：简单训练实体验证可以作 **Starry 安全回退诊断**，但不是跨数据集统一主方法；NASA 仍应报告纯 q，Starry 则以 kNN 为强 support-aware 基线，不能把两者事后拼成一个被验证过的普适算法。

完整原始结果、逐种子配对效应和终态审计见 `runs/hierarchical_q_knn_gate_confirm_20260822/`。
